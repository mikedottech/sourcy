TODO:

	STATE CONTROLLERS:

		--- (X) - Recognize SUPERMOVETIME and PAUSEMOVETIME as optional state controller 
		--- - (AND StateDef) attributes


		(X) - Make parser grammar lookup and link the controller signature to the STATE node
		
		(X) - Make tree parser grammar detect the syntax used
		
		(X) - Generate commas for parsing non-ending optional arguments in controller attributes
		
		(X) - Do something with:
			value = ,HT
			value = SCA
			value =
			(NotHitBy)
		
			Maybe....:
			('value' EQUAL sth) => 'value' EQUAL sth ,,,
			|('value' EQUAL) => 'value' EQUAL

		(X) - kfm720 uses '-' in a HitDef controller in parameter hitFlag
		  check alternative names for controller attributes in kfm720

		(X) - Disambiguate trigger/ID in attributs RHS's
		
		(X) - Make state attribute DB and disambiguate ID/expression
			velset = 0,0
			ctrl = 0
			anim = 200
			sprpriority = 2
			poweradd = 20
			juggle= 4
			facep2= ifelse(prevstateno>=440 && prevstateno<=449,1,0)
			hitdefpersist = 0
			movehitpersist = 0
			hitcountpersist = 0			
				
		(X) - Change ignorehitpause from hardcoded to common
		
		(X) - Fix the 'attr' args in the DB in controllers hitby, nothitby, hitdef, reversaldef, hitoverride
		
		(X) - Place predicates in the tree parser to validate arguments
							
		(X) - Implement anti-duplicates usage array and check for missing mandatory ones
		
		(X) - Force TYPE = ID cr after [State <sth>]
			
		(X) - Fix when the type of a controller is not set!
		
		(X) - Check var()s in lhs to be in varset controllers, and that the rhs is correct
			(X)(!) - Problem: Can't convert to upper a const string
					
		(X) - Typecheck on ^(ASSIGN varReference expr)
			(X)(!) - Problem: The mem. address of common attributes between syntaxes may not match
		
		(X) - Give a warning if the expr inside a var() subscript doesn't evaluate to int / floor it
		
		(X) - Implement CDT_ID validation with valid IDs or letter combinations (also for old-style triggers!)
			(X)(!) - Problem: Need to capture the ID passed from the grammar
			(X)(!) - Problem: Dont quite understand the attr_strings yet
		
		(X) - If the controller is finished and the used syntax is -1, then check if there
			exists a syntax where all of the non-specified attributes are optional and set usedSyntax to it.
		
		(X) - Also check for controllers with 0 syntaxes
		
		(X) - Check for repeats in statedef attributes
		
		(X) - Fix LHS issues when using:		
			varReference = expr
		
		- Do something with IDS in the form S32,10
		
		(X) - VALUE2 as LHS is not recognized
		
		(X) - Validation of old-style triggers
		
		(X) - Check the first arg (S|F thing) in cns.g
		
		(X) - Fix the state controller names issue
	
		(X) - Sort state sons:
				- cntname
				- type
				- triggerall
				- triggerX (sorted by X)
					- state common attrs
					- controller args

		(...) - Accept things present in kfm.cns ?
		
		(X) - Validate fixed statedef attrs
				
	STATES:
		
		(TREE GRAMMAR)		
		(X) - Make sure all required args are present		
		(X) - Validate them all (required and optional)
		(X) - Also disambiguate between trigger and ID
	
	
	GENERAL:
		(X) - Work out intervals
		(X) - Work out vars/fvars/sysvars/sysfvars in varset, varadd....
		- Do something with info / remap / victory .... blocks
	
	CMD:
		- Implement
	
- Fix error reporting and give useful info
	- File/Line
		- Syntax: Expecting one of....
		- Semantics: File/Line + explanation
	- Summary
		- Errors
		- Warnings	
		
=============================================================

